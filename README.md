Рабочая безопасная версия.

# Менеджер AI Агентов (Supabase Edition)

Красивое, готовое к продакшену full-stack приложение для управления записями агентов с возможностями версионирования и полной системой авторизации, использующее Supabase в качестве базы данных.

## 🚀 Возможности

- **Система авторизации**: Полная изолированная система входа/регистрации/восстановления пароля
- **Интеграция с Supabase**: Прямое подключение к Supabase для управления данными
- **Система версионирования**: Автоматическое увеличение версии с группировкой по одинаковому p2_id
- **Интерактивный UI**: Чистый табличный интерфейс с выбором строк и детальным редактированием
- **Обновления в реальном времени**: Оптимистичные обновления UI с обработкой ошибок
- **Функция отмены**: Возможность отменить последние изменения
- **Адаптивный дизайн**: Оптимизирован для десктопа, планшета и мобильных устройств
- **Row Level Security**: Встроенная безопасность на уровне базы данных
- **Готов к продакшену**: Построен с TypeScript, современными React hooks и комплексной обработкой ошибок
- **Изолированная архитектура**: Авторизация и API полностью изолированы для переиспользования

## 🛠️ Технический стек

- **Frontend**: React 18 + TypeScript + Vite
- **Стилизация**: Tailwind CSS с кастомной дизайн-системой
- **База данных**: Supabase (PostgreSQL) с автоматическими миграциями
- **Иконки**: Lucide React
- **Работа с датами**: date-fns
- **Авторизация**: Supabase Auth с email/password

## 📋 Требования

- Node.js 18+ 
- Среда разработки Bolt
- Проект Supabase

## 🔧 Настройка Supabase

### Подключение к Supabase

**ВАЖНО**: Приложение требует авторизации! Только зарегистрированные пользователи могут работать с данными.

#### Настройка проекта:

1. Нажмите кнопку **"Connect to Supabase"** в правом верхнем углу
2. Выберите существующий проект или создайте новый
3. Миграции будут применены автоматически

Структура таблицы **agents**:

| Название поля | Тип    | Описание              |
|---------------|--------|-----------------------|
| id            | uuid   | Первичный ключ        |
| p2_wf_name    | string | Название агента       |
| p2_wf_version | string | Номер версии          |
| p2_id         | string | Уникальный ID агента  |
| p2_variable_1 | string | Функции агента        |
| p2_variable_2 | string | Входные данные агента |
| p2_variable_3 | string | Выходные данные агента|
| p2_variable_4 | string | Резервное поле        |
| created_at    | timestamp | Время создания     |
| updated_at    | timestamp | Время обновления   |

#### Авторизация:

- **Email подтверждение**: ОТКЛЮЧЕНО для простоты разработки
- **Провайдеры**: Только email/password (без социальных сетей)
- **RLS**: Включен для всех таблиц
- **Политики**: Только авторизованные пользователи могут читать/писать данные

**Первый запуск**: Зарегистрируйтесь через форму регистрации для доступа к приложению.

## 🚀 Локальная разработка

1. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```

2. **Приложение будет доступно по адресу:**
   - `http://localhost:5173`

3. **Зарегистрируйтесь** для доступа к функциональности приложения

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── RecordTable.tsx     # Основная таблица данных
│   ├── RecordDetails.tsx   # Интерфейс редактирования записей
│   ├── ErrorNotification.tsx # UI обработки ошибок
│   ├── AuthForm.tsx        # Форма авторизации
│   ├── UserMenu.tsx        # Меню пользователя
│   └── LoadingSpinner.tsx  # Состояния загрузки
├── hooks/              # Кастомные React hooks
│   └── useRecords.ts      # Логика управления записями
├── lib/                # Библиотеки и конфигурация
│   └── supabase.ts        # Клиент Supabase
│   └── auth.ts            # Сервис авторизации
│   └── useAuth.ts         # React хук авторизации
├── services/           # API слой
│   └── api.ts            # Сервис для работы с Supabase
├── types/              # TypeScript определения
│   └── index.ts          # Общие интерфейсы
├── supabase/           # Миграции базы данных
│   └── migrations/        # SQL миграции
└── App.tsx            # Основной компонент приложения
```

## 🔌 База данных

### Авторизация

Приложение использует встроенную авторизацию Supabase:

#### Регистрация пользователя
- Создает нового пользователя с email/password
- Подтверждение email отключено
- Автоматический вход после регистрации

#### Вход в систему
- Проверка email/password через Supabase Auth
- Автоматическое сохранение сессии
- Перенаправление на главную страницу

### Управление данными

Приложение использует Supabase клиент для работы с PostgreSQL:

### Получение записей
- Получает все записи, отсортированные по времени создания (новые первыми)
- Группирует по имени агента, показывает только последние версии
- Возвращает: `AgentRecord[]`

### Получение конкретной записи
- Получает детали конкретной записи
- Возвращает: `AgentRecord`

### Создание записи
- Создает новую запись с автоинкрементом версии
- Автоматически генерирует следующую версию
- Возвращает: `AgentRecord`

### Удаление записи
- Удаляет конкретную запись (используется для функции отмены)
- Используется в функции "Отменить изменения"

**ВАЖНО**: Все операции с данными доступны только авторизованным пользователям благодаря RLS политикам.

## 🔐 Система авторизации

## 📖 Инструкции по использованию

### Описание интерфейса

#### Основные элементы

**Заголовок приложения**
- Меню пользователя с email и кнопкой выхода
- Иконка базы данных и название "Agent Manager"
- Подзаголовок "Manage your AI agents with version control"
- Кнопка "Создать агента" в правом верхнем углу для добавления новых агентов

**Таблица агентов (верхняя часть)**
- Заголовок секции "Все агенты"
- Таблица со следующими столбцами:
  - **Название агента**: Основное имя агента (p2_wf_name)
  - **Версия**: Номер версии в формате X.Y.Z (p2_wf_version)
  - **Функции**: Краткое описание функций агента (p2_variable_1)
  - **Вход**: Входные данные агента (p2_variable_2)
  - **Выход**: Выходные данные агента (p2_variable_3)
  - **Обновлено**: Время последнего обновления в относительном формате
- Выбранная строка выделяется синим цветом с левой границей
- Клик по любой строке выбирает агента для просмотра деталей

**Панель деталей записи (нижняя часть)**
- Заголовок "Детали записи" с кнопками управления справа
- Поля для просмотра и редактирования:
  - **Название агента**: Текстовое поле (обязательное при создании)
  - **Версия**: Только для чтения, автоматически генерируется
  - **Функции**: Многострочное текстовое поле для описания функций
  - **Вход**: Многострочное поле для входных данных
  - **Выход**: Многострочное поле для выходных данных
  - **Резервное поле**: Дополнительное поле для заметок

#### Кнопки управления

**В режиме просмотра:**
- **Edit**: Переключает в режим редактирования
- **Отменить изменения**: Появляется после сохранения, удаляет последнюю версию

**В режиме редактирования:**
- **Подтвердить**: Сохраняет изменения и создает новую версию

**В режиме создания нового агента:**
- **Добавить**: Создает нового агента с версией 1.0.0
- **Отмена**: Отменяет создание и возвращает к просмотру

#### Состояния интерфейса

**Загрузка**
- Анимированные скелетоны в таблице при первой загрузке
- Спиннеры на кнопках во время выполнения операций
- Блокировка кнопок во время обработки запросов

**Ошибки**
- Всплывающие уведомления в правом верхнем углу
- Кнопки "Retry" для повторной попытки
- Кнопка "Dismiss" для закрытия уведомления

**Пустые состояния**
- "No records found" при отсутствии данных
- "Select a record to view details" при отсутствии выбранной записи

### Авторизация

#### Первый вход
1. При первом посещении откроется форма авторизации
2. Нажмите "Зарегистрироваться" для создания аккаунта
3. Введите email и пароль (минимум 6 символов)
4. После регистрации автоматически войдете в систему

#### Последующие входы
1. Введите email и пароль на форме входа
2. Нажмите "Войти"
3. При необходимости используйте "Забыли пароль?"

#### Выход из системы
1. Нажмите на email в правом верхнем углу
2. Выберите "Выйти" в выпадающем меню

### Просмотр записей
1. Записи отображаются в сортируемой таблице с новыми записями первыми
2. Кликните на любую строку для просмотра детальной информации
3. Выбранная запись выделяется синим акцентом

### Редактирование записей  
1. Нажмите кнопку **"Edit"** в секции деталей записи
2. Измените любые поля в интерфейсе формы
3. Нажмите **"Confirm"** для сохранения изменений
4. Автоматически создается новая версия с тем же `p2_id`

### Управление версиями
- Каждое сохранение создает новую запись с увеличенной версией
- Предыдущие версии остаются неизменными в базе данных
- Номера версий автоинкрементируются (например, 1.0.1 → 1.0.2)

### Отмена изменений
1. После подтверждения изменений появляется кнопка **"Undo Changes"**
2. Нажмите для удаления последней созданной записи
3. Предыдущая версия снова становится активной

### Создание нового агента
1. Нажмите кнопку **"Создать агента"** в правом верхнем углу
2. Заполните все необходимые поля в форме
3. Нажмите **"Добавить"** для создания нового агента
4. Новый агент создается с версией 1.0.0

### Обработка ошибок
- Сетевые ошибки отображают уведомление с опцией повтора
- Все сбои API показывают понятные пользователю сообщения об ошибках
- Валидация формы предотвращает пустые обязательные поля

## 🔒 Функции безопасности

- **Row Level Security**: Автоматическая защита данных на уровне базы
- **Авторизация**: Только зарегистрированные пользователи имеют доступ
- **Сессии**: Безопасное управление пользовательскими сессиями
- **Политики доступа**: Настроенные правила для аутентифицированных пользователей
- **Валидация данных**: Клиентская валидация всех входных данных
- **Обработка ошибок**: Корректная обработка сетевых ошибок и ошибок API
- **Типобезопасность**: Использование TypeScript для предотвращения ошибок типов

## 🎨 Дизайн-система

- **Цвета**: Основной синий (#3B82F6), успех зеленый (#10B981), предупреждение янтарный (#F59E0B), опасность красный (#EF4444)
- **Отступы**: 8px сетка для консистентных макетов
- **Типографика**: Системный шрифтовой стек с правильной иерархией
- **Компоненты**: Консистентные стили кнопок, полей ввода и состояний обратной связи
- **Адаптивность**: Mobile-first дизайн с брейкпоинтами на 768px и 1024px

## 🧪 Тестирование

Приложение включает комплексную обработку ошибок и состояния загрузки для всех пользовательских взаимодействий. Тестовые сценарии включают:

- Сетевые сбои и таймауты API
- Обработка невалидных данных
- Ошибки авторизации и регистрации
- Истечение сессий пользователей
- Одновременные изменения пользователей
- Обновление браузера в режиме редактирования
- Операции отмены с валидацией

## 📚 Зависимости

### Продакшн зависимости
- `react` + `react-dom`: UI фреймворк
- `@supabase/supabase-js`: Клиент Supabase
- `lucide-react`: Библиотека иконок
- `date-fns`: Утилиты форматирования дат
- `date-fns`: Утилиты форматирования дат

### Зависимости разработки  
- `typescript`: Типобезопасность
- `tailwindcss`: Фреймворк стилизации
- `vite`: Инструмент сборки и dev сервер
- `eslint`: Качество кода

## 🗄️ Миграции базы данных

Миграции находятся в папке `supabase/migrations/` и применяются автоматически при подключении к Supabase.

### Текущие миграции:
- Создание таблицы агентов с индексами и RLS
- Настройка политик безопасности для авторизованных пользователей

## 🤝 Участие в разработке

1. Следуйте существующей структуре кода и соглашениям об именовании
2. Поддерживайте соответствие TypeScript strict mode
3. Добавляйте правильную обработку ошибок для всех асинхронных операций
4. Создавайте новые миграции для изменений схемы базы данных
5. Тестируйте авторизацию и безопасность перед развертыванием
5. Тестируйте всю функциональность перед развертыванием

## 📄 Лицензия

Этот проект настроен для продакшн использования с учетом корпоративного уровня безопасности и масштабируемости с использованием современных технологий Supabase.
